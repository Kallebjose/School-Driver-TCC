<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Cor de fundo do botão -->
    <corners android:radius="12dp" /> <!-- Arredondamento dos cantos -->
    <stroke
        android:width="2dp"
        android:color="@color/black" /> <!-- Borda do botão (opcional) -->
</shape>
